# 凭证推送工具 使用说明

在 PC 上把本机各平台的登录凭证同步到 NAS 签到面板，**不用记命令**。

## 日常使用（二选一，双击即可）

| 双击文件 | 作用 |
|---|---|
| `推送凭证.bat` | 全量模式：wb-switch 账号文件 + 桌面端直读 + inbox 存档 → 推送 |
| `推送凭证-免wb.bat` | 不依赖 workbuddy-switch：Trae/WorkBuddy 桌面端直读 + inbox 存档 → 推送 |

看到每行 `平台 → main: OK`（或 `→ 新增账号: OK`）即成功。

## 多账号怎么加

**桌面客户端平台（Trae / WorkBuddy）**：
1. 在客户端里切换到另一个账号登录
2. 双击 bat —— 工具发现新的账号身份（JWT uid），自动作为**副账号**追加进面板，主账号不受影响
3. 想同时留两个号的凭证：先在 wb-switch 里存好两个号，用全量模式 bat 一次推齐

**浏览器 Cookie 平台（WPS / 百度搭子 / MiniMax / 魔搭）**：
1. 命令行运行一次（会弹浏览器，登录你要的账号；需先 `pip install playwright` 并
   `playwright install chromium`）：
   `python tools\push_panel.py --login wps,dazi,minimax,modelscope`
2. **这类平台在面板上只有一个主账号**：换号登录再推送会直接覆盖主账号的票，
   想要面板上同时留两个号，只能在设置页手动粘贴副号凭证
3. 抓取时工具会真发一次平台只读接口验票，服务端不认的残票/过期票不会再被导出

**Qoder**：本地存储加密读不了，需抓包 `openapi.qoder.com.cn` 复制
`Authorization: Bearer` 后的 `dt-` 串，粘贴到面板设置页保存。

## 身份路由规则（工具自动判定，无需操作）

- 有 JWT 身份的票（Trae 等）：换了票同一号 → **原位刷新**；身份是新的 → **自动追加**副账号
- 只有 Cookie 的平台（WPS / 搭子 / MiniMax / 魔搭 / WorkBuddy）：一律钉死**主账号原位刷新**，永不自动建副号
- 面板拉取失败（断网/密码错）→ **中止推送**，防止重复建号

## 命令行参数（可选）

```
python tools\push_panel.py                # 常规推送
  --list          只预览每个凭证会落到哪个账号，不发送
  --all           忽略"7 天新鲜度"限制，inbox 全部再推一遍
  --no-wb         完全不读 wb-switch（即"免wb"bat 的行为）
  --login a,b     推送前先弹浏览器刷新指定平台 Cookie
  --url http://x  临时指定面板地址（默认 nas.zeln.top:8000）
```

## 文件清单

| 文件 | 说明 |
|---|---|
| `tools\push_panel.py` | 推送工具本体（Python 3，无第三方依赖，需 PATH 有 python 和 openssl） |
| `tools\.panel_token` | 面板访问密码（已配好；换密码时更新此文件） |
| `推送凭证.bat` / `推送凭证-免wb.bat` | 双击入口 |
| `D:\data\inbox\` | 凭证暂存目录（login 抓取与手动粘贴都落这里，7 天内可重推） |

## 常见问题

- **失败: URLError** → 不在家/面板域名解析不到，连回内网或确认 nas.zeln.top 解析
- **失败: HTTPError 401** → `tools\.panel_token` 里的密码和 NAS `.env` 的 `PANEL_TOKEN` 不一致
- **副账号没出现** → 该客户端没登录到新账号；先在客户端里确认已切换成功再双击
- 面板上删账号：设置页 → 对应账号行 → 删除；下次双击推送不会误恢复（凭证源头已删则不会回来）
